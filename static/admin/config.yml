backend:
  name: github
  repo: "shayeqkhan01/opencommonplace-blog"
  branch: main
  base_url: "https://auth.opencommonplace.com"
  auth_endpoint: "auth"

media_folder: "static/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "blog"
    label: "Blog"
    folder: "content/posts"
    create: true
    slug: "{{slug}}"
    fields:
      - label: Title
        name: title
        widget: string

      - label: Date
        name: date
        widget: datetime
        format: "YYYY-MM-DDTHH:mm:ssZ"

      - label: Description
        name: description
        widget: text
        required: false

      - label: Featured Image
        name: image
        widget: image
        required: false

      - label: Body
        name: body
        widget: markdown